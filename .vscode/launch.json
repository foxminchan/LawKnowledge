{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch api",
      "request": "launch",
      "type": "node",
      "runtimeExecutable": "npx",
      "runtimeArgs": ["nx", "serve", "api"],
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
